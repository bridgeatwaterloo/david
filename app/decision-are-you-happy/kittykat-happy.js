var meow = new audio("cat.mp3")

function kitty() {
	if showIndicatorView = ('.decision-are-you-happy');
		audio.currentTime = 0;
		meow.play;
		int = setInterval(function() {
    if (audio.currentTime > 0.4) {
    	audio.pause();
   		clearInterval(int);
		}
	}, 20);
}